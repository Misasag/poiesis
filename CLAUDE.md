@AGENTS.md
Use the local `harness@poiesis-harness` plugin for development orchestration.
Entry point: `node tools/harness/harness/bin/hx.mjs`; start with `status` and `route`.
Use `run`, `verify`, and a different-family `judge`, then record `outcome`.
Read `tools/harness/harness/docs/README.md` for budgets, benches, dogfood, and frozen evaluation.
