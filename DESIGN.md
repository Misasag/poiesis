# Poiesis Agent UIデザインガイド

> 状態: 改訂2・承認済み、Agent画面へ反映済み（2026-09-07）

この文書は承認されたAgent画面の視覚改訂と、その実装基準を記録しています。
[実画面](docs/ui/assets/agent-revision-2-implemented.png)は、ビルドした本体に検証用の会話を読み込んで撮影したものです。
[比較プレビュー](docs/ui/agent-design-review.html)は承認時のレビュー資料として保持しています。プレビュー内の「提案中」「製品コード未変更」は、その時点の記録です。
「現行ベース」は実装済みスタイルから再構成した比較用モックで、実画面のスクリーンショットではありません。

## 対象と不変条件

対象は会話レール、ヘッダー、会話本文、作業履歴、Composerです。
Agent／Results／Code、ワークスペース、セッション、会話、成果の役割と配置は維持します。
比較の両表示で同じ会話、選択、下書き、レール状態、作業履歴、スクロール位置を共有します。

## 視覚方針

- 暗色ニュートラルの静かな作業面を基調にし、暖色はフォーカス表示に限って使います。
- セッション名と回答本文を先に読めるようにし、場所情報と履歴は一段弱くします。
- 通常のナビゲーションと作業履歴は枠で囲まず、整列、文字差、余白でまとまりを示します。
- Composerだけを1つの入力面として持ち上げ、その内側のツール群には外周枠を付けません。
- 選択面やタブの一辺だけに色線を置き、角丸へ回り込ませる表現は禁止します。

## 参照からの適用

Refero上の画像を観察資料として使っています。公式ブランド仕様やトークンとして引用していません。

| 画面で見える特徴 | Poiesisで選んだ適用 | 比較プレビューの対応セレクター |
| --- | --- | --- |
| Linear製品画面の密度ある枠なしナビ行 | アイコン、ラベル、時刻を揃え、選択は中立な全面塗りにする | `.product[data-variant="proposed"] .session` |
| Linear製品画面の見出し・本文・低優先度履歴の差 | 17pxの短い導入、16px本文、12px履歴に分ける | `.agent-message .lead`, `.agent-message`, `.activity` |
| ChatGPT画面の枠なしサイドナビゲーション | 通常操作を独立カードにせず、枠を増やさない既存配置を維持する | `.rail-action`, `.sessions` |
| ChatGPT画面の単一Composer面 | Composerだけを境界付き面にし、内部ツールのグループ枠をなくす | `.composer`, `.tool-group`, `.tool-chip` |

Linearは参照画像下部の製品画面だけを対象とし、マーケティング見出しや光彩は採用しません。
ChatGPTはログアウト状態の明色画面であり、配色、ログイン操作、音声機能は採用しません。

## 色

以下はRefero由来の値ではなく、Poiesisの改訂2用に選んだ値です。

| 役割 | 値 | 用途 |
| --- | --- | --- |
| Canvas | `#181918` | 会話とアプリの基底 |
| Rail / Surface | `#20211f` | 左レールと静かな補助面 |
| Composer | `#242522` | 唯一の持ち上げた入力面 |
| Hover | `#2a2b28` | ポインターホバー |
| Selection | `#2c2d2a` | 選択行と選択タブの全面塗り |
| Hairline | `#30322d` | レール、ヘッダー、Composerの1px境界 |
| Strong hairline | `#484a44` | Composerの通常境界 |
| Primary text | `#eeeee9` | セッション名、見出し、主要ラベル |
| Body text | `#d2d3cd` | 回答本文 |
| Muted text | `#a6a8a0` | 場所情報、通常メタデータ |
| Quiet text | `#999c94` | 時刻など重複性のある情報 |
| Focus | `#c28b60` | キーボード焦点リング、入力中のComposerの境界 |
| Success | `#acbba1` | アイコンと状態名を伴う完了表示 |

意味のある小さい文字は、実際に載る背景とのコントラスト比4.5:1以上を保ちます。
状態は色だけに依存せず、アイコンと「完了」などのテキストを併記します。

## タイポグラフィ

外部フォントを読み込まず、既存のシステムフォントスタックを使います。

```css
Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI",
"Yu Gothic UI", "Hiragino Sans", "Noto Sans JP", Meiryo, sans-serif
```

| 役割 | サイズ / 行高 | ウェイト |
| --- | --- | --- |
| 回答本文 | `16px / 1.75` | `400` |
| 回答の短い導入 | `17px / 1.65` | `540` |
| Composer入力 | `14px / 1.55` | `400` |
| ナビゲーション、セッション名 | `13px / 1.4–1.48` | `400–560` |
| メタデータ、作業履歴 | `12px / 1.4–1.55` | `400–520` |

改訂前の本文はすでに15pxでした。改訂2では本文と補助情報の差を明確にするため、回答本文を16pxへ上げています。
本文幅は最大784pxとし、日本語を不自然に詰めません。

## 寸法と状態

- 通常レールは232px、狭い比較表示では216px、折りたたみ時は52pxです。
- ヘッダーは50px、主要コントロールは30px以上、Composer内のツールは28px、Composerは12px角丸です。
- 基本余白は`4 / 8 / 12 / 16 / 24 / 32px`とし、会話本文には約24pxの節間を設けます。
- セッションはタイトルと時刻を同じ行へ置き、選択中だけ完了状態を下段へ残します。
- 作業履歴は枠なしの開閉行と整列した明細行で表示します。
- Agentタブと選択セッションは中立な全面塗りと文字ウェイトで示し、片側線や片側影は使いません。
- Composerは`1px solid #484a44`と弱い影を持ち、内部の作業場所・モデル・成果は枠なしで横並びにします。
- 焦点は`2px solid #c28b60`、外側オフセット2pxで示します。

## 実装状況

比較プレビューは、表示切り替え、レール折りたたみ、作業履歴の展開、編集可能な下書き、参照画像ダイアログを含みます。
参照ダイアログは製品UIの外側にあり、閉じるボタンとEscapeに対応し、閉じた後は起点へ焦点を戻します。
本体はAgent用の配色、文字、余白、選択表示、枠なしの作業履歴、単一面のComposerへ変更しました。共有レールとヘッダーの視覚変更はAgentモードに限定し、既存の状態管理とアクセシビリティ属性を保っています。

実製品では既存のレール幅変更と自動折りたたみを維持します。通常幅232pxの検証では、1600×900と1280×800で本文・Composerとも784px、1024×768で本文750px・Composer752pxでした。非選択の実行中・失敗・キャンセル・下書きなどの状態も表示します。既存会話のワークスペースとブランチは静的表示とし、新規会話だけ従来のワークスペース変更を使えます。

ブラウザー版とElectron版のビルド、ソース検証、Composer・作業履歴・回答の画像／コード表示のテストが通過しました。実画面では3サイズの収まり、履歴の開閉、レールの折りたたみ、モデル選択、Agent／Results／Code間の下書き保持、焦点表示、失敗表示を確認しました。小さい文字の最小コントラスト比は4.97:1です。実CLIを起動する実行中状態の再試験は行っていません。

## 参照

- [LinearのRefero参照ページ](https://styles.refero.design/style/90ce5883-bb24-4466-93f7-801cd617b0d1) / [ローカル画像](docs/ui/assets/refero-linear.jpg)
- [ChatGPTのRefero参照ページ](https://styles.refero.design/style/52a007ed-ad1b-46a6-bd44-b76f91df6d0c) / [ローカル画像](docs/ui/assets/refero-chatgpt.jpg)
